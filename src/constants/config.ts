import { Platform } from 'react-native';

export const CONFIG = {
  // Backend URL
  // We are using 'adb reverse tcp:8080 tcp:8080' so localhost works on Android too
  API_BASE_URL: 'http://localhost:8080',
};
