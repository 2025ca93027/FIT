services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    image: fit-api
    container_name: fit_api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      CONNECTIONSTRINGS__DEFAULT: Host=db;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      JWT__KEY: ${JWT__KEY}
      JWT__ISSUER: ${JWT__ISSUER}
      JWT__AUDIENCE: ${JWT__AUDIENCE}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrate
    image: fit-migrate
    container_name: fit_migrate
    restart: "no"
    environment:
      RUN_MIGRATIONS: "true"
      ASPNETCORE_ENVIRONMENT: Production
      CONNECTIONSTRINGS__DEFAULT: Host=db;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:18-alpine
    container_name: fit_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer
    container_name: adminer
    restart: always
    ports:
      - "8081:8080"
    depends_on:
      - db

  scalar:
    image: scalarapi/api-reference
    container_name: fit_scalar
    restart: unless-stopped
    environment:
      API_REFERENCE_CONFIG: '{"sources":[{"name":"FIT API","url":"http://localhost:8080/openapi/v1.json"}]}'
    ports:
      - "8082:8080"
